import { CardScript, CardContext } from '../engine/scripting/types/CardScriptTypes';

/**
 * Playful Phantom
 * Unit - Common - Calm
 * Cost: 5 energy
 * Might: 5
 *
 * Description: . (vanilla unit, no special effects)
 *
 * Tags: Shadow Isles, Spirit
 *
 * Implementation:
 * - Vanilla unit with no special effects
 * - No card script needed (no hooks)
 * - This file exists for consistency but contains no logic
 */
const playfulPhantom: CardScript = {
  // No hooks needed for vanilla units
  // The card enters play and functions normally without special effects
};

export default playfulPhantom;
