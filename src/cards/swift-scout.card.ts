import { CardScript, CardContext } from '../engine/scripting/types/CardScriptTypes';

/**
 * Swift Scout
 * Unit - Common - Fury
 * Cost: 1 energy
 * Might: 1
 *
 * Description: A cheap scout unit for early board presence.
 *
 * Tags: Demacia, Scout
 *
 * Implementation:
 * - Vanilla 1-cost unit
 * - Good for testing early game plays with minimal resources
 */
const swiftScout: CardScript = {
  // No hooks needed for vanilla units
};

export default swiftScout;
